{
    "require": {
        "phpunit/phpunit": "*",
        "programmes-plant-modules": "dev-master"
    },
    "repositories": [
        {
            "type": "git",
            "url":  "git@github.com:gnozu/programmes-plant-modules.git"
        }
    ],

    "minimum-stability": "dev",
    
    "config": {
        "bin-dir": "bin/"
    }
}