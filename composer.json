{
    "require": {
        "phpunit/phpunit": "*",
        "unikent/programmes-plant-modules": "dev-master"
    },
    "repositories": [
        {
            "type": "git",
            "url":  "git://github.com/unikent/programmes-plant-modules.git"
        }
    ],

    "minimum-stability": "dev",
    
    "config": {
        "bin-dir": "bin/"
    }
}