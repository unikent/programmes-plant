<?php

//require_once path('base') . 'vendor/autoload.php';

class PERMISSIONSFIX_Task {

    /**
     * Fix permissions without ug prefix
     * 
     * @param array  $arguments The arguments sent to the command.
     */
    public function run($arguments = array())
    {
        // TODO: 
        // 1. Get all broken permissions
        // $permissions = \Verify\Models\Permission::all();
        // print_r($permissions);
        // 2. Add the ug prefix to them
    }
    
}
    
    